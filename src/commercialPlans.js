// Source gallery order differs from the floor numbers printed on its boards.
export const seoulFloors=[3,4,5,6,7,8,9,10,12,11,13,0].map((source,index)=>({floor:index+1,reference:`seoul-${source}.jpg`}));
